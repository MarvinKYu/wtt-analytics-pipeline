name: wtt_analytics
version: '1.0.0'
config-version: 2

profile: wtt_analytics

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  wtt_analytics:
    staging:
      +schema: wtt
      +materialized: view
    intermediate:
      +schema: wtt
      +materialized: view
    marts:
      +schema: wtt
      +materialized: table
